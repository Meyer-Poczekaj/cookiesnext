{
  "name": "cookiesnext-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w next-cookie-consent",
    "test": "npm run test -w next-cookie-consent",
    "dev": "npm run build -w next-cookie-consent && npm run dev -w demo",
    "build:demo": "npm run build -w next-cookie-consent && npm run build -w demo"
  }
}
